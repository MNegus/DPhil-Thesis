function PlatePositions = platepositions(ws, w_ts, w_tts)
%%platepositions
% Loads the positions of the plate solutions into a structured array
    PlatePositions.ws = ws;
    PlatePositions.w_ts = w_ts;
    PlatePositions.w_tts = w_tts;
end